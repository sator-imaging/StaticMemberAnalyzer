<!--
**IMPORTANT**: THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
-->

# SatorImaging.MeticulousAnalyzer.Analysis
Auto-generated by `DocsGen, Version=5.2.0.0, Culture=neutral, PublicKeyToken=null`  
Resource file: `./src/analysis/Resources.resx`  
Resource suffix: `_Title` `_Description` `__MD_TITLE__` `__MD_DESC__`  

## Static Field Analysis

Analyzes static field declarations and initialization order to prevent reading uninitialized or cross-referenced static values.

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0001 | Reading Uninitialized Value                                      | Reading '{0}' before initialization. Reorder declarations
| SMA0002 | Cross Referencing across Type                                    | Reading '{0}' that cross-references static field in '{1}'. Initialization order is undefined and value is unstable
| SMA0003 | Static Member Declared in Another File                           | Reading '{0}' that is declared in another partial file. Initialization order is undefined and value is unstable
| SMA0004 | Late Declaration                                                 | Earlier-declared '{0}' reads '{1}' before declaration. Move it after that

## `TSelf` Type Arg Analysis

Analyzes `TSelf` generic type arguments and constraints to ensure self-referencing type parameters are correctly declared.

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0010 | `TSelf` is Not Self                                              | `TSelf` should point to itself (Did you mean '{0}'?)
| SMA0011 | `TSelf` is Not Self or Base                                      | `TSelf` should point to itself or base type (Did you mean '{0}'?)
| SMA0012 | `TSelf` is Not Self or Derived                                   | `TSelf` should point to itself or derived type (Did you mean '{0}'?)
| | | |
| SMA0015 | `TSelf` Constraint is Not Self                                   | `TSelf` constraint should point to itself (Did you mean '{0}'?)

## Enum Type Analysis

Analyzes enum casts, string conversions, obfuscation settings, and enum-like patterns.
> Suppression comment: `// Allow enum conversion [ Reason (optional) ]`

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0020 | Unchecked Cast to Enum Type                                      | Unchecked value cast to enum type '{0}'. Centralize and verify in a utility method. Can suppress with comment `// Allow enum conversion [ Reason (optional) ]`
| SMA0021 | Cast from Enum Type to Other                                     | Casting enum type '{0}' to another type. Centralize in a utility method. Can suppress with comment `// Allow enum conversion [ Reason (optional) ]`
| SMA0022 | Unchecked Cast to Generic Enum Type                              | Unchecked value cast to generic enum type '{0}'. Centralize and verify in a utility method. Can suppress with comment `// Allow enum conversion [ Reason (optional) ]`
| SMA0023 | Cast from Generic Enum Type to Other                             | Casting generic enum type '{0}' to another type. Centralize in a utility method. Can suppress with comment `// Allow enum conversion [ Reason (optional) ]`
| SMA0024 | Enum to String                                                   | String representation of enum type '{0}' is not localized and unstable on obfuscation. Centralize enum-to-string handling. Can suppress with comment `// Allow enum conversion [ Reason (optional) ]`
| SMA0025 | Enum System Method                                               | Centralize enum system method calls in a utility class. Can suppress with comment `// Allow enum conversion [ Reason (optional) ]`
| SMA0026 | Enum Obfuscation                                                 | Add `Obfuscation` attribute to prevent enum field name changes on obfuscation
| SMA0027 | Unusual Enum Definition                                          | Non-Flags enum should use `int` type without explicit index values
| SMA0028 | Incomplete Enum-like Pattern                                     | Enum-like pattern implementation of type '{0}' is incomplete

## Struct Analysis

Analyzes struct constructors, readonly field assignments, and implicit boxing conversions.
> Suppression comment: `// Allow boxing [ Reason (optional) ]`

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0030 | Invalid Struct Constructor                                       | Use the explicit constructor for '{0}' instead of parameter-less default
| SMA0031 | Mutable Struct Field marked as Read-Only                         | Mutable struct type '{0}' should not be set to `readonly` field
| SMA0032 | Implicit Boxing Conversion                                       | Implicit boxing from type '{0}' to '{1}'. Consider using generic constraint. Can suppress with comment `// Allow boxing [ Reason (optional) ]`

## Disposable Analysis

Analyzes `IDisposable` implementation and usage patterns to ensure proper resource management and disposal.
> Suppression comment: `// Don't dispose [ Reason (optional) ]`

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0040 | Missing Using Statement                                          | Type '{0}' implements `IDisposable` pattern. Wrap with `using` statement. Can suppress with comment `// Don't dispose [ Reason (optional) ]`
| SMA0041 | Null Assignment to Disposable                                    | Disposable object '{0}' is assigned `null` without a preceding `.Dispose()` or `?.Dispose()` call
| SMA0042 | Not All Code Paths Return a Value                                | Disposable object '{0}' is not returned on all code paths. Can suppress with comment `// Don't dispose [ Reason (optional) ]`
| SMA0043 | Undisposed Member                                                | Disposable type '{0}' is not disposed. Add disposal in `Dispose` method. Can suppress with comment `// Don't dispose [ Reason (optional) ]`
| SMA0044 | Missing Dispose Implementation                                   | Type '{0}' owns disposable members but does not implement `Dispose` method
| SMA0045 | Missing IDisposable Interface                                    | Type '{0}' owns disposable members. Implement `IDisposable` interface

## File Structure Analysis

Analyzes source file headers and file structural conventions.

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0050 | Missing File Header Comment                                      | File should start with a header comment (`//` or `/* ... */`)

## Read-Only Variable Analysis

Analyzes local variables and parameters to enforce immutability and prevent unintentional reassignments or mutations.

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0060 | Avoid Reassigning Locals                                         | Local '{0}' is immutable. Rename to 'mut_{0}' to allow reassignment
| SMA0061 | Avoid Reassigning Parameters                                     | Parameter '{0}' is immutable. Rename to 'mut_{0}' to allow reassignment
| SMA0062 | Avoid Passing Writable Variables to Calls                        | Variable '{0}' is immutable. Cannot pass as writable argument
| SMA0063 | Property Access May Change State                                 | Property '{0}' may mutate immutable '{1}'. Rename to 'mut_{1}' to allow access
| SMA0064 | Method Invocation May Change State                               | Method '{0}' may mutate immutable '{1}'. Rename to 'mut_{1}' to allow invocation

## Async Context Analysis

Analyzes asynchronous task usage to ensure tasks are properly awaited or returned.
> Suppression comment: `// Don't await [ Reason (optional) ]`

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0070 | Task Not Awaited                                                 | Task type '{0}' is not awaited. Add `await` or `return`. Can suppress with comment `// Don't await [ Reason (optional) ]`
| SMA0071 | Task Not Awaited on All Paths                                    | Task type '{0}' is not awaited on all code paths. Can suppress with comment `// Don't await [ Reason (optional) ]`

## Project Structure Analysis

Analyzes cross-namespace access restrictions to enforce internal architecture boundaries.

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0080 | Internal Cross-Namespace Access                                  | Cannot access internal member '{0}' declared in namespace '{2}' from namespace '{1}'. Internal types and members must not be accessed from a different namespace, including parent or sibling namespaces. (Exceptions: `Core` leaf namespace and `SR` type)

## `MoveOnly` / `NoCopy` Type Analysis

Analyzes move-only or `NoCopyAttribute`-marked types to enforce single-ownership semantics and prevent prohibited copies, assignments, casts, or captures.

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA0090 | Invalid MoveOnly Type                                            | MoveOnly type '{0}' must declare a public instance `Move()` method returning the containing type
| SMA0091 | Prohibited Copy of MoveOnly Type                                 | MoveOnly type '{0}' cannot be copied or assigned without calling `Move()`
| SMA0092 | Prohibited Ref/In/Out Parameter of MoveOnly Type in Async Method | MoveOnly type '{0}' cannot be passed by `ref`, `in` or `out` in `async` methods
| SMA0093 | Invalid MoveOnly Declaration                                     | MoveOnly type '{0}' must be a struct
| SMA0094 | Prohibited Cast of MoveOnly Type                                 | MoveOnly type '{0}' cannot be cast to '{1}' without calling `Move()`
| SMA0095 | Prohibited Lambda Capture of MoveOnly Type                       | MoveOnly type '{0}' cannot be captured in a lambda expression
| SMA0096 | Prohibited Out Parameter of MoveOnly Type                        | MoveOnly type '{0}' cannot be declared as an `out` parameter
| SMA0097 | Prohibited Return of MoveOnly Type                               | MoveOnly type '{0}' cannot be returned without `ref` outside its `Move()` method

## Coding Assistance

Provides rules and assistance to improve runtime efficiency, avoid delegate allocations, and ensure NativeAOT compatibility.
> Suppression comment: `// Allow allocation [ Reason (optional) ]`

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA7000 | Unmarked Static Lambda                                           | Lambda declaration can be `static`
| SMA7001 | Inefficient Delegate Declaration                                 | Implicit conversion to '{0}' delegate is handled inefficiently on older runtime
| SMA7002 | Lambda Allocation                                                | Non-static lambda causes allocation. Make it static or cache if possible. Can suppress with comment `// Allow allocation [ Reason (optional) ]`
| | | |
| SMA7010 | Reflection Access                                                | Reflection operation '{0}' involves type '{1}'. Access to members of types from `System.Reflection` is not NativeAOT-friendly
| SMA7011 | Reflection Type Variable                                         | Variable '{0}' is declared with reflection type '{1}'. This operation is not NativeAOT-friendly
| | | |
| SMA7020 | Avoid `AggressiveInlining` to Public Member                      | Avoid applying `AggressiveInlining` to public member '{0}' that may cause unpredictable side effects on consumer side. Potential downsides include code bloat, increased instruction cache misses, reduced branch prediction accuracy, performance gains limited to microbenchmarks, etc
| | | |
| SMA7030 | Implicit `params` Array Allocation                               | Implicit `params` array allocation for '{0}' hides actual overhead from source code representation. Use explicit array allocation
| | | |
| SMA7040 | Avoid Anonymous Object Creation                                  | Prefer tuple instead of anonymous object

## Reliability and Maintainability Analysis

Provides rules for argument readability, explicit typing, exception handling, and branch condition safety.
> Suppression comment: `// Ignore exception: Reason (required)`
> Suppression comment: `// Allow non-local exit from loop [ Reason (optional) ]`
> Marker comment: `// Early exit [ Description (optional) ]`

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA8000 | Literal Should Be Passed as Named Argument                       | Pass literal for '{0}' as named argument to express their meaning (Methods in the System namespace with only one argument are exempt)
| SMA8001 | Explicit Number Declaration                                      | Declare system number primitive '{0}' with explicit type '{1}' instead of `var`
| SMA8002 | Clearer Null Suppression Operation                               | Null suppression should be fenced with 3 parentheses for visibility, or use `Debug.Assert({0} is not null);` for safe null check
| SMA8003 | Debug-only Assert in Public API                                  | Avoid using debug-only assertion in public API surfaces, as it can lead to undefined behavior in Release builds
| SMA8004 | Omittable Argument Should Be Passed as Named Argument            | Pass omittable argument for '{0}' as named argument to express their meaning
| | | |
| SMA8010 | Catch Block without Throw                                        | Catch block does not contain a `throw` statement. Can suppress with leading comment for `catch` keyword: `// Ignore exception: Reason (required)`
| SMA8011 | Catch-All Block without Throw                                    | Catch-all block suppresses all exceptions and should contain a `throw` statement, or catch only specific exceptions instead
| | | |
| SMA8020 | Literal Branch Number                                            | Use named constants or configuration instead of hardcoded literal '{0}' for better maintainability. Can suppress by adding a trailing comment after the literal (e.g. `value == 42 /* Why: reason */`)
| SMA8021 | Literal Branch Zero                                              | Use named constants or configuration instead of hardcoded literal '{0}' for better maintainability. Can suppress by adding a trailing comment after the literal (e.g. `value == 0 /* Why: reason */`)
| SMA8022 | Literal Branch String                                            | Use named constants or configuration instead of hardcoded string literal '{0}' for better maintainability. Can suppress by adding a trailing comment after the literal (e.g. `value == "whatever" /* Why: reason */`)
| SMA8023 | Literal Branch Char                                              | Use named constants or configuration instead of hardcoded literal char '{0}' for better maintainability. Can suppress by adding a trailing comment after the literal (e.g. `value == 'a' /* Why: reason */`)
| | | |
| SMA8030 | Incomplete Mid-Flow Branch                                       | Avoid adding exit statements, such as `return`, `throw`, `yield`, `goto`, `break`, or `continue` in the middle of the main control flow. Instead, use a complete `if-else` statements without, or with exits in all code paths, or extract methods to clarify the control flow. If `if-only` statement is not detected as early exit block unexpectedly, add marker comment before `if` keyword to avoid error: `// Early exit [ Description (optional) ]`
| SMA8031 | State Change in Early Exit                                       | Avoid state changes in the early exit block. Declarations of local variables and up to 1 method call that is not expected to have any side effects are permitted. Use a helper method to combine multiple method calls into one and decrease the number of calls
| SMA8032 | Non-Local Exit from Loop                                         | Avoid non-local exit from the loop. Use local exit such as `break`, `continue` or `goto` instead to clarify the control flow. Can suppress with comment `// Allow non-local exit from loop [ Reason (optional) ]`

## `Obsolete` Annotating and Underling

Analyzes for IDE underlining and visual annotations.

| ID      | Diagnostic                                                       | Description
|---------|------------------------------------------------------------------|-------------
| SMA9000 | Underlining on Identifier Symbols                                | Draw underline in IDE
| SMA9001 | Underlining on Local Variables                                   | Draw underline in IDE
| SMA9002 | Underlining on Method or Lambda Parameters                       | Draw underline in IDE
| | | |
| SMA9010 | Underlining on Declarations                                      | Draw underline in IDE
| | | |
| SMA9015 | Underlining only on Designated Type                              | Draw underline in IDE
| | | |
| SMA9020 | Underlining at Line Head                                         | Draw underline in IDE
| SMA9021 | Underlining at Line Leading                                      | Draw underline in IDE
| SMA9022 | Underlining on Identifier                                        | Draw underline in IDE
| SMA9023 | Underlining at Line End                                          | Draw underline in IDE
| | | |
| SMA9100 | Underlining as Warning                                           | Draw underline in IDE
